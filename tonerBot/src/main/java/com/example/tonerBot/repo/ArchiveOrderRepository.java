package com.example.tonerBot.repo;

import com.example.tonerBot.model.ArchiveOrder;
import org.springframework.data.repository.CrudRepository;

public interface ArchiveOrderRepository extends CrudRepository<ArchiveOrder, Long> {
}
